
# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
if ( -f "/opt/miniconda3/etc/profile.d/conda.csh" ) then
    source "/opt/miniconda3/etc/profile.d/conda.csh"
else
    setenv PATH "/opt/miniconda3/bin:$PATH"
endif
# <<< conda initialize <<<

